'use strict';

function isType(item, kind) {
  return item.kind === kind;
}
function isEvent(item) {
  return isType(item, "event");
}
function isCall(item) {
  return isType(item, "call");
}
function isTranasction(item) {
  return isType(item, "transaction");
}
function isEvm(item) {
  return isType(item, "evmLog");
}
function isNotEmpty(value) {
  return value !== null && value !== void 0;
}

exports.isCall = isCall;
exports.isEvent = isEvent;
exports.isEvm = isEvm;
exports.isNotEmpty = isNotEmpty;
exports.isTranasction = isTranasction;
exports.isType = isType;
